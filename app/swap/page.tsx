import SwapWidget from '@/components/shared/SwapWidget'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <SwapWidget />
    </div>
  )
}

export default page